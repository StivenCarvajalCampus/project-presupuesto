/*let informacion = [
    document.querySelector('#current_dinner'),
    document.querySelector('#div_renevu'),
    document.querySelector('#div_expenses')

]
export default{
    render_localStorage(){
        let types = ['current_dinner','div_renevu','div_expenses']
        for(let x=0; x < types.length; x++){
            if(!localStorage.getItem(types[x]))return
            let tpm = JSON.parse(loccalStorage.getItem(types[x]))
            informacion[x].innerHTML=tpm
        }
    }, 
}*/